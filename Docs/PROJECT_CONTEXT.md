# 재의 길 — 프로젝트 컨텍스트

> 마지막 갱신: 2026-09-24

이 문서는 새 작업이나 새 대화에서 프로젝트의 현재 상태를 빠르게 파악하기 위한 요약입니다. 과거 작업 과정은 `DEVELOPMENT_LOG.md`에만 남기고, 여기에는 현재도 유효한 사실과 다음 행동만 유지합니다.

## 2026-09-24 작업 요약 (PR #1~#10, 전부 main 병합)

| PR | 내용 |
|---|---|
| #1 | 궁극기 영상 검은 화면 — 원인은 VideoPlayer **Skip On Drop**(게임이 멈춘 실시간 재생에서 중간 프레임을 전부 버림). 빌더가 손으로 만든 판을 지우던 `GameObject.Find` 버그 |
| #3 | 사용자 로컬 작업 병합 — 재의 창, 왕관 의식, 페이즈 전환 B, 소품 무작위 배치, 툴 메뉴 7묶음 |
| #2 | 투사체 풀링(`ProjectilePool`, 내장 `ObjectPool<T>`) — 화살·사수 화살·재의 창. 왕관 의식을 못 막으면 궁극기 영상 컷인 재생. 복구용 임시 씬(`_Recovery`) 삭제·gitignore |
| #4 | 풀에서 다시 꺼낸 화살의 파티클 예외(Stop Action Destroy), 의식·그로기·전환 중 보스가 밀려나던 것(`FreezeAll`) |
| #5 | 이미 끝난 항목(궁극기 모션 가림 09-06, 사수 화살·자폭병 새 그림 09-16)을 완료로 표시 |
| #6 | R 폭발이 캐릭터를 덮던 것 — 폭발이 있는 동안 시전자를 폭발 위에(`CasterSortingLift`) |
| #7 | R 무릎 꿇기를 폭발 끝까지 유지(`HoldPoseWhile`, Animator.speed 0) — 사용자 평: 애매하지만 유지 |
| #8 | 결과 화면 제목 줄바꿈, Title·GameClear를 4의 배수로(BC7 적용), 한글 폰트 Dynamic 전환 도구 |
| #9 | 화면보다 큰 UI 그림 Max Size(규칙 판 3), UI 아이콘 스프라이트 아틀라스 도구 |
| #10 | 보스 열쇠는 8번째 방부터(`ChestRelicReward.keyMinRoom`) |

**유니티에서 해야 할 것 / 확인할 것**
- `그림 → 한글 폰트를 Dynamic으로`(실행함) — 한글이 네모·번짐 없이 나오는지, 콘솔에 찍힌 메모리 전후 숫자 기록
- `그림 → UI 아이콘 아틀라스 만들기` 실행 — Frame Debugger로 인벤토리 아이콘 그리기 명령이 묶였는지
- `씬·세팅 → 궁극기 컷인 게임 씬에 추가`(Game 씬, 이미 했으면 생략)
- UI 게이지·인벤토리 칸·보스 열쇠 패널이 흐려지지 않았는지(Max Size)
- 보스 열쇠가 7번째 방까지 안 나오는지. 8번째 방부터 4개 모으기가 너무 길면 `keyChance`(현재 0.25)를 0.4~0.5로

**다음 후보**: 남은 이펙트 풀링(궁극기 폭발·자폭병·스킬 이펙트), 쓰지 않는 원화·백업 정리(136MB), 사운드(맨 마지막)

## 한 줄 요약

영구 성장 없이 유물 시너지로 한 판을 완성하는 2D 탑다운 픽셀아트 던전 슬래셔 로그라이크입니다.

## 설계에서 바꾸지 않을 것

- 죽으면 언락·영구 강화 없이 런을 완전히 초기화합니다.
- 반복 동기는 유물 조합과 전투 숙련이 담당합니다.
- 재시작은 상태를 수동 초기화하지 않고 게임 씬을 다시 로드하는 방식입니다.
- 게임플레이 아트는 회색·숯색을 바탕으로 주황 잉걸만 강조색으로 씁니다.
- 공격은 피해보다 예비동작과 회피 가능성이 먼저 읽혀야 합니다.
- 에디터 도구는 메뉴에서 명시적으로 실행하며 씬을 자동 저장하지 않습니다.
- **방의 경계 벽은 그 방이 소유합니다.** 씬 최상위에 공유 벽을 두지 않습니다 — 방은 통째로
  켜고 꺼지므로 벽이 방 안에 있으면 켜고 끄는 코드가 필요 없고, 규격이 다른 방이 생겨도
  어긋나지 않습니다. 2026-09-09 이전에는 전역에 하나뿐이라 보스 방에서 못 나갔습니다.
- **출구 판정은 그 방 벽의 안쪽 면에 붙어 있습니다.** 떨어져 있으면 문에 닿기도 전에 나가지고,
  벽 너머면 영원히 안 닿습니다.
- 기존 Unity 에셋을 교체할 때 `.meta`와 GUID를 유지합니다.
- 저장 금지의 대상은 게임 진행(영구 성장·언락)이지 플레이 환경이 아닙니다. 볼륨·화면·키 설정은 저장합니다.

## 현재 플레이 흐름

1. `Title`에서 게임 시작
2. `Game`의 **튜토리얼 방**에서 조작을 익히고 아래 문으로 나감
3. 던전 방 전투 → 적 전멸 후 보상 상자 → 개봉 후 문과 출구 활성화
4. 상자에서 **보스 열쇠** 4종을 모으면, 다음 보상을 챙길 때 **부서진 문**이 열림
5. 부서진 문으로 나가면 **보스 방** 진입
6. 보스 처치 → 클리어 유물 "길의 매듭" 드롭 → 획득 시 문 개방 → 문 통과 → **클리어 연출 「손을 펴다」(약 2.7초, 카메라가 다가감)** → 승리로 런 종료
7. 플레이어가 죽으면 패배로 런 종료
8. `Result`에서 결과 표시 및 재시작

튜토리얼 방과 보스 방은 별도 씬이 아니라 `Game` 씬 안의 방입니다. 씬을 나누면 HUD·인벤토리·플레이어를 복제해야 하고, 복제본이 원본과 어긋나는 위험이 생깁니다.

보스 진입 조건은 방 번호가 아니라 **열쇠**입니다. 두 조건을 같이 두면 열쇠를 다 모았는데 방 번호가 먼저 걸려 보스가 나오고, 어느 쪽으로 갔는지도 알 수 없어서 방 번호 조건은 걷어냈습니다.

**승리 흐름은 2026-09-09에 걸어서 완주까지 확인했습니다.** 보스 처치 → 유물 → 문 통과 →
결과 화면이 조사용 도구 없이 이어집니다.

열쇠를 다 모으면 **그 방에서 바로** 부서진 문이 열립니다. 예전에는 보상을 한 번 더 챙겨야 했는데,
상자가 "열렸다"와 "보상을 손에 넣었다"를 신호 둘로 나누면서 같이 풀렸습니다.

## 구현된 시스템

### 코어와 전투

- `GameFlow`, `RunManager`, `RunResultData`로 씬 흐름과 런 결과 관리
- `Health`, `DamageInfo`, `DamageHitbox`를 플레이어와 적이 공유
- 플레이어 이동, 대시, 넉백과 실제 속도 기반 애니메이터 연동
- Ctrl 기본 공격과 Q/W/E/R 스킬
- `SkillData` 상속 구조로 근접·투사체·장판 스킬 분리

### 방과 적

- 잿불 망령: 배회 → 추격 → 예비동작 → 돌진 → 회복 상태 머신
- `ObjectPool<T>` 기반 적 재사용
- `RoomController`, `RoomDoorState`, `RoomExitTrigger`, `RoomSequenceController`
- 전멸 → 상자 → 문 개방 순서를 이벤트로 연결
- 방 이동 시 플레이어의 Rigidbody2D 속도 초기화

### 유물과 인벤토리

- 유물 14종: 일반 10종과 보스/진행 유물 4종
- `RelicData`는 정의, `RelicInstance`는 런마다 결정된 실제 수치를 소유
- 유물 수치는 정의된 범위 안에서 런마다 무작위 결정
- 보관함과 장착 슬롯 3개 분리
- **보스 열쇠 4종은 전용 칸 4개**를 따로 씀 — 진행 표식이 전투 칸을 뺏으면 열쇠를 모을수록 약해집니다
- `T`로 여는 보스 열쇠 화면. 칸을 눌러 빼면 보관함으로 돌아가고 사라지지 않습니다
- 장착된 유물만 효과 적용
- 장착 변경 시 기본값에서 모든 보정치를 다시 계산
- `Health`와 `PlayerStamina`는 기본 최대치 + 유물 보정 구조
- 인벤토리를 열면 시간이 멈추며 `I` 또는 `Tab`으로 닫음

### 입력

- 조작 액션 전부를 `InputBindings`가 소유합니다. 컴포넌트는 꺼내 읽기만 합니다
- 액션 맵은 부팅 때 켜고 끄지 않습니다 — 켜져 있어도 읽는 쪽이 없으면 아무 일도 안 일어납니다
- **인스펙터에서 키를 바꿀 수 없습니다.** 그 자리는 설정 화면입니다. 두 곳에서 바꾸면 어느 쪽이 맞는지 정할 수 없습니다
- `InputCatalog`는 조작의 이름과 묶음만 들고, 키 문구는 실제 액션에서 읽습니다
- 이동은 **방향키만**입니다. `W`가 스킬 2와 겹쳐서 WASD를 뺐습니다
- 기본값: 방향키 이동 / `Shift` 대시 / `Ctrl` 기본 공격 / `Q W E R` 스킬 / `F` 상자 / `I`·`Tab` 보관함 / `T` 열쇠 / `ESC` 설정
- `ESC`는 바꿀 수 없습니다. 옮길 수 있게 하면 창에서 못 나오는 상태를 스스로 만들 수 있습니다
- 키 겹침은 **같은 맥락끼리만** 따집니다. 스킬 4와 결과 화면 재시작은 둘 다 `R`이지만 두 화면이 동시에 뜨지 않습니다
- 겹치면 **막지 않고 비웁니다.** 이미 쓰는 키라고 거절하면 왜 안 되는지 모른 채 다른 키를
  계속 눌러보게 됩니다. 비우고 무엇을 비웠는지 알려주면 "저기도 다시 정해야겠구나"가 보입니다
- **키를 뺏긴 자리는 비어 있을 뿐 사라지지 않습니다.** 그 자리에 다시 아무 키나 넣을 수 있어야
  합니다. 2026-09-04까지는 그러지 못했습니다 — 자리를 `effectivePath`로 찾다 보니 비워진 자리를
  건너뛰어 `-1`이 나왔고, 키보드 자리가 하나뿐인 스킬 `Q W E R`은 한 번 뺏기면 **초기화 말고는
  되돌릴 방법이 없었습니다.** 지금은 `path`(원래 경로)로 자리를 찾습니다
- **키를 글자로 적어둔 UI가 없어야 합니다.** 스킬바·튜토리얼·결과 화면은 전부 실제 바인딩에서 읽습니다

### 설정과 시간 정지

- `PauseGate`가 `Time.timeScale`을 만지는 **유일한 곳**입니다. 열린 화면을 세고 스택이 빌 때만 시간을 되돌립니다
- 화면을 전환할 때는 **먼저 열고 뒤에 닫습니다.** 반대로 하면 스택이 잠깐 비어 시간이 튑니다
- `SettingsScreen`은 일반/조작 두 탭이고 Title과 Game이 같은 화면을 씁니다
- Game은 `ESC`로 열고, Title은 구석의 버튼으로 엽니다. 타이틀의 `ESC`는 예전부터 게임 종료라 겹칠 수 없습니다
- 창 크기는 해상도 목록이 아니라 **정수 배수**(2/3/4배)입니다. 비정수배는 픽셀이 일렁입니다
- `GameSettings`가 `PlayerPrefs`에 저장하고 부팅 때 한 번 적용합니다
- **볼륨은 아직 들을 소리가 없습니다.** `GameAudioMixer`를 만들면 배선 없이 살아납니다
- **창 크기·전체화면은 에디터에서 확인할 수 없습니다.** Game 뷰가 `Screen.SetResolution`을 무시합니다

### 재의 왕 보스

- 1페이즈: 거대한 중갑 재의 왕
- 체력 **70%**에서 갑옷이 무너지고 여성형 진체인 2페이즈로 전환.
  두 페이즈의 몫을 일부러 어긋나게 나눴습니다 — 1페이즈가 전체의 30%(27), 2페이즈가 70%(63).
  반반이면 2페이즈가 "체력이 한 번 더 있는 1페이즈"가 되는데, 실제로 어려운 쪽은 뒤입니다
- 전환은 **Timeline 3.125초 연출**(`BossTransition.playable`)입니다. 시간표의 주인이
  타임라인 에셋 하나뿐이라 코드와 클립의 시간이 갈라질 자리가 없습니다.
  `BossTransitionSequence`는 재생과 시그널 중계만 하고 시간을 세지 않습니다
- 2페이즈 핵심 의미: 갑옷 속 인물이 아니라 재가 왕의 형상을 직접 얻은 상태
- 거리 기반 패턴 선택과 페이즈별 AnimatorController 분리
- 패턴 3종 — 내려찍기(옆으로 피함), 잿불 파도(사이로 피함), **재 폭발**(멀어져서 피함)
- 재 폭발은 2페이즈 전용. 반경 12로 방 바닥 대부분을 덮고 **네 모서리만 안전**하다
- 판정은 그림(좌우 2방향)이 아니라 **실제 방향**을 따름 — 내려찍기 상자를 조준 방향으로 회전
- 공격 시간은 **클립 길이 그대로**, 2페이즈는 `phase2MotionScale`(0.8)로 줄임 (2P 클립이 15fps)
- 2페이즈에서 몸통 콜라이더 **폭만** 0.87배로 줄임 (시트를 재보면 높이는 같고 폭만 줄었다)
- 보스 프리팹: `Assets/Project/Prefabs/Enemies/BossAshKing.prefab`
- 보스 로직: `Assets/Project/Scripts/Enemy/EnemyBoss.cs`
- `EnemyBoss.OnValidate`가 인스펙터 값의 모순을 경고한다 — 시전 사거리가 물러나는 거리보다
  작으면 그 패턴은 영영 안 나오고, 닿는 거리보다 크면 시전해도 헛친다
- **죽는 한 대는 `OnDamaged`에서 돌려보낸다.** `Health`가 `Damaged → Changed → Died` 순으로
  알리기 때문에, 안 막으면 사망 순간에 페이즈 전환이 시작돼 무적을 켜고 잘린다

### 보스 체력바와 이름

- `BossHealthBar`는 **대상을 스스로 찾지 않고 `Bind`로 받습니다.** 보스는 방에 들어가는 순간
  만들어지는 인스턴스라, 게임 시작 시점에는 씬을 뒤져도 존재하지 않습니다
- `BossEncounter`가 보스를 만든 직후 물리고, 방을 정리할 때 뗍니다. 재 게이지를 잇는 방식과
  같습니다 — 저쪽은 **플레이어가** 프리팹이라, 이쪽은 **보스가** 프리팹이라 미리 못 겁니다
- **바는 체력 전체가 아니라 지금 페이즈의 몫만 그립니다.** 지금 값인 0.7이면 1페이즈는
  체력 100~70%를 바 100~0%로, 2페이즈는 체력 70~0%를 다시 바 100~0%로 옮깁니다.
  그래서 전환 직전에 바가 **완전히 비고**, 전환이 끝나면 **다시 가득 찹니다**.
  두 페이즈의 몫이 30 대 70으로 어긋나 있어도 바에는 안 드러납니다 — 둘 다 가득 찬 바에서
  0까지 내려갑니다
- 전환 지점을 눈금으로 예고하던 방식은 2026-09-02에 걷어냈습니다. 눈금은 "곧 온다"까지만
  말하지만, **바가 0이 되는 것은 이겼다고 믿게 만듭니다.** 그 믿음이 뒤집히는 것이 2페이즈입니다
- **경계값은 보스에게 물어서 넘깁니다.** UI에 0.5를 적어두면 보스 수치를 바꿨을 때 바가
  옛 기준으로 나뉘고, 그건 에러 없이 밸런스 문제로 오해됩니다
- 이름은 1페이즈 `???`, 2페이즈 `재의 길`입니다. **`???`의 답은 칭호가 아니라 이름이어야
  합니다** — 이 세계에는 고유명이 하나도 없어서(전부 분류명), 하나만 이름을 가지면
  형식만으로 "종류가 아니라 개체"가 전달됩니다
- 2페이즈 이름이 게임 제목인 이유: 재는 **앞서 이 길을 걷다 죽은 것들의 재**입니다.
  죽으면 완전 초기화라는 규칙이 시스템이 아니라 세계관이 됩니다
- 페이즈 신호 경로: `EnemyBoss.EnteredPhase2` → `BossEncounter` → `BossHealthBar.MarkPhase2()`.
  **보스는 화면 구조를 모릅니다.** 사망을 `Health`가 알리고 방이 받는 것과 같은 분리입니다
- `EnteredPhase2`는 전환이 **끝날 때** 웁니다. 시작할 때 울리면 갑옷이 무너지는 것을 보기 전에
  바 색이 먼저 바뀌어 결과를 미리 말합니다
- 프레임은 **게이지 슬롯이 아래쪽 절반**에 있습니다(위 212px이 뿔 장식, 높이의 51.5%).
  프레임 기준으로 위치를 잡으면 항상 "생각보다 낮다"가 됩니다
- 바 폭의 한계를 정하는 것은 **왼쪽 체력바**입니다. 화면 중앙 정렬이라 가운데 최대 폭이 606입니다

## 아트와 임포트 규칙

- 캐릭터 스프라이트는 Point 필터, 압축 없음, Mesh Type `Full Rect`
- **플레이어 발바닥은 셀 안 `y=216`입니다**(맨 아래 불투명 행, 0부터 셈). 8방향 시트 10장이 전부
  이 값이고 `NormalizeDirectionalSheets.ps1`의 `-GroundLine` 기본값도 216이라 그대로 쓰면 됩니다.
  2026-09-11 정정 — 여기 한동안 "241, 항상 -GroundLine 241"로 적혀 있었는데 틀린 값이었습니다.
  241은 09-09 `_ArtReview` 걷기 시험본에만 쓴 값이고, 그대로 따르면 시트가 25px 가라앉습니다
- **스프라이트 피벗은 모든 칸이 같은 커스텀 값**입니다(`alignment: 9`, 아래에서 39px = 발바닥 줄).
  스프라이트마다 계산하지 않으므로 **캐릭터가 256px 셀 안 어디에 그려지느냐가 곧 화면에서 서는 높이**입니다
- 셀 안 캐릭터 실측: **141 × 190px**(셀 세로의 74%). 월드 5.94유닛, 화면 높이의 19%.
  원본 1픽셀이 화면 1픽셀에 가깝게 찍히므로 **원본을 더 크게 뽑아도 화면에서 안 살아납니다**
- 기준 PPU `32`, 기준 해상도 `640×360`, 카메라 orthographic size `5.625`
- 스프라이트 시트는 균등 셀로 재조립하고 발바닥 기준선을 맞춤
- 보스 시트는 `1536×256`, 셀 `256×256`, 가로 6프레임
- 보스 캐릭터는 정면 전시 자세가 아니라 플레이어처럼 몸을 튼 3/4 전투 자세 사용
- 초록 키 배경은 `#00FF00`; 캐릭터와 효과에는 초록 계열 금지
- 픽셀아트는 검은 외곽선, 2~3단 셀 셰이딩, 하드 엣지 사용

현재 보스 시트 경로:

- `Assets/Project/Art/Characters/Boss/AshKing/`
- 플레이어형 정리본: `Raw/PlayerLike/Normalized/`
- 이전 정면 버전 백업: `Raw/FrontFacingBackup/`

## 중요한 에디터 도구

- `AshProjectSetup` — 레이어, 충돌, 카메라와 프로젝트 기본 설정
- `AshSpriteImportRules` — PPU, 필터, 압축 등 임포트 규칙 강제.
  **규칙을 바꾸면 `GetVersion()`의 숫자를 같이 올려야** 기존 에셋이 다시 임포트됩니다
- `AshTextureFormatAudit` — `Tools → 재의 길 → 조사 → 텍스처 포맷 감사`. 임포터 설정이 아니라
  **임포트된 텍스처의 실제 포맷**을 읽어 포맷별 장수·메모리와 "4의 배수가 아니라 압축이
  막힌 것"을 뽑습니다. 압축 관련 작업을 하면 이걸로 확인합니다
- `AshRoomProgressionBuilder` — 방 진행 구성
- `AshRelicBuilder` / `AshRelicIconProcessor` — 유물 데이터와 아이콘 처리
- `AshInventoryUiBuilder` — 인벤토리 UI 구성
- `AshGameHudBuilder` — 게임 HUD 전체 구성. **전체 생성은 HUD를 통째로 지우고 다시 만듭니다.**
  씬에서 맞춘 값을 지키려면 `보스 체력바만 생성 (나머지 HUD 유지)`를 쓰거나, 값을 상수에 넣습니다
- `AshGaugeArtProcessor` — 게이지 원본의 흰/초록 배경을 알파로 바꾸고 여백을 잘라냅니다.
  잘라낸 뒤 **안쪽 채움 영역을 로그로 알려줍니다** — 그 숫자가 `AshGameHudBuilder`의 앵커가 됩니다
- `AshBossPrefabBuilder` — 보스 프리팹과 애니메이션 구성
- `AshTitleSceneBuilder` — 타이틀 연출 구성
- `AshBossRoomBuilder` — 보스 방 생성, 배경·배치 맞추기.
  **벽·입장 지점·출구를 배경 스프라이트 경계에서 다시 계산합니다.** 배경 상수를 바꾸면
  방 크기가 통째로 바뀝니다. `InteriorInsetPixels = 100`은 2026-08-21판 정사각 그림을
  재서 나온 값이라, 지금 쓰는 08-26 가로 그림과 여백 비율이 다를 수 있습니다
- `AshRoomWallBuilder` — **방마다 자기 벽을 갖게 하고 씬 최상위 공유 벽을 걷어냅니다.**
  이미 자기 벽이 있는 방(보스 방)은 크기를 안 건드리고 레이어만 맞춥니다 — 방마다 규격이
  다른 것이 정상이라 통일하면 보스 방을 다시 좁히게 됩니다. 출구 판정도 벽 안쪽 면에 맞춥니다.
  한 번 돌리면 끝이지만 여러 번 돌려도 안전합니다
- `AshTutorialRoomBuilder` — 튜토리얼 방 생성, 설정·배경·출구 재적용
- `AshSettingsUiBuilder` — 설정 화면 구성. `설정 화면 전체 구성 (Title + Game)`으로 두 씬을 한 번에
- `AshTrainingDummyBuilder` — 허수아비 레이어·콜라이더·컴포넌트 배선
- `AshBossWaveBuilder` — 잿불 파도 투사체 생성과 보스 연결
- `AshPlayerDirectionalAnimationBuilder` — 8방향 슬라이스, 클립 80개, 블렌드 트리
- `AshSceneLightingBuilder` — URP 2D 조명과 Lit 머티리얼 적용
- `AshTopDownSortingBuilder` — Y축 정렬 축, 정렬 기준점, 소품 충돌체
- `AshGroundShadowBuilder` — 캐릭터 접지 그림자

에디터 도구는 자동 실행·자동 저장하지 않습니다. 실행 후 씬 변경을 확인하고 사람이 직접 저장합니다. 대부분 여러 번 실행해도 안전하도록 "이미 있으면 참조와 설정만 다시 채우는" 방식으로 만들었습니다.

## 조사용 도구 (플레이 중)

`Assets/Project/Scripts/Debug/` 아래. **전부 파일 전체가 `#if UNITY_EDITOR`로 감싸여 있어
빌드에는 클래스 자체가 없습니다.** 인스펙터 체크박스로 끄는 방식은 켜둔 채로 빌드하는
실수가 언젠가 반드시 나오기 때문입니다. 씬에 손대지 않고 `RuntimeInitializeOnLoadMethod`로
실행 시점에 스스로 붙습니다.

| 키 | 하는 일 |
| --- | --- |
| `F1` | **디버그 패널** — 판·플레이어·보스·씬·성능을 한 화면에. 기본은 꺼져 있습니다 |
| `F3` | 보스 열쇠 전부 지급 |
| `F6` | 플레이어 무적 |
| `F7` | 배속 순환 (1 → 0.5 → 0.25 → 0.1) |
| `F8` | 시간 멈춤 / 재개 |
| `F9` | 한 프레임 진행 (멈춘 상태에서) |
| `F11` | 재 게이지 가득 (F10은 유니티 Recorder의 녹화 단축키라 피했습니다) |
| `B` | 보스 방으로 점프 |
| `K` | 즉시 사망 |
| `1` `2` `3` | 문 상태 (닫힘 / 열림 / 부서짐) |

**디버그 패널(F1)에서 볼 수 있는 것 중 특히 쓸모 있는 것:**

- **보스가 2페이즈 전환까지 몇 대 남았는지.** 그 연출은 이 프로젝트에서 손이 제일 많이 간
  부분인데, 확인하려면 매번 보스를 27 깎아야 했습니다. `보스를 2페이즈 직전으로` 버튼은
  임계값 **한 칸 위**에 세웁니다 — 직접 때려서 전환이 시작되는 그 프레임부터 보라는 뜻입니다
- **`PauseGate` 스택 개수.** 화면을 다 닫았는데 이 숫자가 0이 아니면 그게 곧 2026-09-04에
  났던 소프트락입니다. 증상이 "게임이 멈췄다"로만 나타나 원인을 찾는 데 제일 오래 걸린
  종류라, 숫자 하나로 즉시 갈립니다
- **프레임 시간의 최근 최악값.** 평균으로는 133~233ms 히치가 안 보입니다
- **보스 열쇠 개수.** 열쇠 HUD가 아직 미구현이라 지금은 여기서만 보입니다

**시간 제어(F7~F9)를 만든 이유:** 이 저장소에서 반복된 버그는 전부 *"몇 초에 무엇이
보이는가"*였습니다(클립 길이 vs 코드 동작 시간, 이펙트가 캐릭터를 덮음, 전환 마지막 프레임).
지금까지 그걸 찾은 방법은 플레이를 녹화해서 ffmpeg으로 프레임을 뜯는 것이었고, 정확하지만
**고치고 다시 확인하는 데 한 판이 통째로** 들었습니다. 0.1배속이면 10fps 클립의 한 프레임이
실시간 1초가 되어 눈으로 셀 수 있습니다.

주의 — 시간 제어는 `PauseGate`와 부딪히지 않게 **활성일 때 매 프레임 다시 먹이는** 방식입니다.
`PauseGate`가 화면을 닫을 때 `Time.timeScale = 1f`로 하드 설정하기 때문입니다. 그리고 그
계산에 `PauseGate`의 상태를 그대로 존중합니다 — 화면이 열려 있으면 무조건 0입니다.

## 스프라이트 시트 도구 (`Tools/`)

PowerShell로 돌리는 이미지 전처리 도구입니다. 원본은 각 폴더의 `Raw/` 아래에 백업됩니다.

- `NormalizeBossSheets.ps1` — 보스 시트 발 라인 정렬, 빈 프레임 복구. `-Only`로 시트 하나만 처리한다
  (전체를 다시 돌리면 이미 정규화된 그림이 `Raw/PreNormalize` 백업을 덮어써 원본을 잃는다)
- `NormalizeDirectionalSheets.ps1` — 8방향 시트(6~8열 × 8행) 셀별 발 라인 정렬
- `InstallPlayerSheets.ps1` — 정규화한 시트를 기존 파일명 자리에 설치(클립 GUID 유지)
- `DefringeCharacterSprites.ps1` — 실루엣에 구워진 밝은 테두리 제거
- `PrepareBossRoomSprite.ps1` — 보스 방 배경의 크로마키 초록 배경을 투명으로 변환
- `RemoveChromaKeyGreen.ps1` — 캐릭터 시트의 크로마키 초록 배경 제거. 발 라인 정렬 전에 먼저 돌린다
- `NormalizeVfxSheets.ps1` — VFX 시트 바닥선 정규화, 피벗을 접지선으로 교체
- `MirrorDamagedPlayerDirections.ps1` — 손상된 방향 행을 대칭 짝의 좌우 반전으로 대체
- `RemoveCrossFrameFragments.ps1` — 칸 경계를 넘어온 이펙트 조각 제거.
  2026-09-11 수정: 칸 오른쪽 절반(x ≥ 128)의 좌표를 `[int]` 반올림으로 잘못 풀어 오른쪽 조각을 한 번도 못 잡았다
- `RecolorToEmberPalette.ps1` — 빨강을 팔레트(숯색 + 주황 잉걸)로 옮긴다. 눈은 크기·자리·주변으로 찾아 지킨다.
  두 번 걸면 채도가 누적으로 빠지므로 빨강이 적은 시트는 건너뛴다(`-Force`로 강제)
- `RemoveFloatingFragments.ps1` — 몸에서 떨어져 허공에 뜬 작은 조각을 지운다. 스킬·VFX 시트에는 쓰지 않는다(불티가 정상)
- `NormalizeGeneratedStrip.ps1` — 생성 원본(3x2 격자·한 줄)을 256 셀 6프레임 한 줄로. 같은 배율, 발바닥 216, 칸 가운데 정렬
- `AssembleDirectionalSheet.ps1` — 5방향 한 줄 + 좌우 반전 3방향 → 8방향 시트 설치. 마지막 프레임은 대기 그림.
  사망처럼 마지막 프레임에서 멈추는 모션은 `-KeepLastFrame`(대기로 안 바꾼다)
- `NormalizeVfxStrip.ps1` — 생성 VFX 한 줄(투명 배경 또는 초록 배경)을 256 셀 6프레임으로. 알파 64 미만 번짐 제거,
  프레임마다 **변하지 않는 요소**(바닥 타원 가운데·화살촉·출발점·흰 심지)를 셀의 같은 자리에 모은다.
  C# "원본 시트 정규화"는 투명 원본을 못 읽는다(초록이 아닌 픽셀을 전부 불투명으로 만든다). 09-14에 쓴 인자는 개발 로그에 표로 있다
- `NormalizeVfxSheets.ps1` — 09-14부터 기본 대상이 비어 있다. 옛 기본값(Q 두 장)은 새 그림이라 이 도구로 다시 맞추면 자리가 틀어진다

도구는 모두 미리보기가 기본이고 `-Apply`를 붙여야 실제로 쓴다. 원본은 각 폴더의 `Raw/` 아래에 백업된다.

## 2026-09-11 — 새 캐릭터 색 맞춤, 기본 공격 8방향 교체

- 09-10에 넣은 새 캐릭터(걷기·대기)는 불투명 픽셀의 56%가 진홍색이라 팔레트 규칙과 어긋났다.
  다시 뽑지 않고 `RecolorToEmberPalette.ps1`(시안 B)로 옮겼다. 빨강 56% → 0.1%, 눈은 빨간색 유지
- 걷기·대기의 허공 조각(몸에서 12~36px 떨어진 세로 조각) 43개를 지웠다
- **기본 공격(Ctrl)을 새 캐릭터로 교체했다.** 5방향 생성, W·SW·NW는 반전. 흐름:
  생성(3x2 격자, `_ArtReview/20260911-attack-v1/prompt_attack_*.txt`) → `NormalizeGeneratedStrip` →
  `RecolorToEmberPalette -Force -NeutralSaturation 1.0` → `AssembleDirectionalSheet -Apply` →
  `RemoveFloatingFragments -MaxPixels 150`(옆 칸 칼끝 제거). 시트 크기·격자가 같아 빌더는 다시 안 돌렸다
- 생성 프롬프트 요령: 칼 방향을 프레임마다 시계로 못박아야 자세가 갈린다. "칼끝을 아래로" 같은
  칸 맞춤 조건은 모든 프레임을 대기 자세로 끌고 간다. 크기는 요구해도 안 지키므로 후처리 몫이다
- ~~아직 옛 치비 캐릭터인 시트: bow, staff, sword_slam, ultimate, dash_hit, hit, death~~ → hit는 09-12, 나머지는 09-13에 교체
- **기본 공격 VFX는 초안만 있다**(`output/imagegen/ember-slash-six-frame-draft.png`). 붙이려면 VFX 한 줄,
  프리팹, `Skill_Basic_AshSlash.effectPrefab` 연결, `SkillData.SpawnEffect`의 발 높이 배치를 가슴 높이로
  올리는 코드 수정이 필요하다

## 2026-09-13 — 스킬·대시·사망 모션 교체, 궁극기 VFX

- **플레이어 8방향 시트 10장이 전부 새 캐릭터다.** 프롬프트 `_ArtReview/20260913-<모션>-v1/`, 원본 `Art/NewPlayerImages/<모션>/`
- 생성 프롬프트에는 서브컬쳐(애니 가챠 액션 RPG) 톤 문단이 들어간다. 다음에 뽑는 시트·VFX도 이 문단을 유지한다
- 사망 시트는 마지막 프레임이 잿더미다. 정규화는 `-StandingFrame 0`, 조립은 `-KeepLastFrame`
- `RunManager.resultDelaySeconds` = 1.4(사망 클립 0.75초 + 히트스톱 + 잿더미 여유). **씬에 직렬화된 값**이라 코드 기본값만 고치면 안 먹는다
- **궁극기 VFX는 새 시트 `vfx_kings_ember_crown_6frames_1536x256`(피벗 가운데)이다.** 옛 `vfx_kings_ember_6frames_1536x256`은
  `BossEmberWave`가 계속 쓴다. `AshBossWaveBuilder`를 다시 돌리면 KingsEmber에서 새 그림을 복사해 가니 주의
- 위쪽 방향 일부는 칸에 맞추려고 키를 줄였다(Q N 173, 활 NE 175·N 180, 궁극기 NE 185, 나머지 190)
- ~~남은 VFX(대시·마법·Q·활)는 생성 원본만 있고 교체 전이다.~~ → 09-14에 7장 전부 붙였다(아래)

## 2026-09-14 — Q·W·E·대시 VFX 7장, 입력 없는 대시 방향

**생성 원본이 있던 VFX는 이제 전부 게임에 붙어 있다.** R(궁극기)은 09-13에 이미 붙었다.

| 키 | 붙는 곳 | 시트 (`Art/Sprites/VFX/`) | 프리팹 | 기준점(피벗) | 배율 · fps |
| --- | --- | --- | --- | --- | --- |
| Q 1단 | `GroundSlamSkillData.nearEffect` | `vfx_sword_slam_impact` (덮어씀) | `SlamImpact` | 타원 가운데 = 지면선 | 1.8 · 14 |
| Q 2단 | `GroundSlamSkillData.farEffect` | `vfx_sword_slam_forward_burst` (덮어씀) | `SlamBurst` | 균열선 가운데 = 지면선 | 2 · 14 |
| W 비행 | `ProjectileSkillData.projectilePrefab` | `vfx_ember_arrow_flight` (덮어씀) | `EmberArrow` | **화살촉**(Center → Tip) | 0.85 · 16 루프 |
| W 발사 | `Skill_W.effectPrefab` (앞 2.5, 높이 1.2 = 발사 지점) | `vfx_ember_arrow_release` (새로) | `EmberArrowRelease` | 출발점(왼쪽 28px) | 0.7 · 20 |
| W 명중 | `Projectile.impactEffectPrefab` | `vfx_ember_arrow_impact` (안 쓰던 것을 덮어씀) | `EmberArrowImpact` | 가운데 | 0.7 · 20 |
| E | `AreaSkillData.effectPrefab` | `vfx_ash_staff_ground_spell` (덮어씀) | `AshPillar` | 마법진 가운데 = 지면선 | 2.5 · 7 |
| 대시 | `PlayerController.dashEffectPrefab` (높이 1) | `vfx_dash_burst` (새로) | `DashBurst` | 출발점(왼쪽 28px) | 1.2 · 16 |

- 덮어쓴 시트는 `.meta`·GUID를 그대로 둬서 프리팹 참조가 안 바뀌었다. 옛 그림은 `Art/Sprites/VFX/Raw/PreNewCharacterVfx/`
- **명중은 `DamageHitbox.HitLanded` 이벤트로 받는다.** 판정 규칙(레이어·중복·무적)은 히트박스에만 있고, 투사체는 걸러진 결과만 듣는다.
  이펙트는 적 콜라이더(발밑)가 아니라 **화살촉 자리**에 터진다 — 화살은 발사 높이만큼 위에 그려지기 때문이다
- 대시 자국은 캐릭터 자식이 아니라 월드에 남는다(출발점 표시). 회전만으로 8방향 — 그림이 위아래 대칭이라 뒤집혀도 같다
- **입력 없이 대시하면 바라보는 8방향으로 나간다.** 예전에는 좌우 bool로만 나가서, 위를 보고 서서 Shift를 누르면
  몸은 옆으로 미끄러지고 그림은 위로 돌진했다. bool(`facingRight`)과 속성(`FacingRight`)은 걷어냈다
- C# 정규화 표에서 위 다섯 시트 줄을 뺐다. 남겨두면 "원본 시트 정규화"(전체)가 옛 원본으로 새 그림을 조용히 덮는다
- 새 시트 2장의 `.meta`는 손으로 만들었지만 **슬라이서와 같은 ID 규칙**을 따른다 — `internalID = spriteID(GUID)의 해시`.
  VFX 폴더의 슬라이서 산출물 67칸 전부가 이 규칙이라, 규칙대로 만들면 "VFX 스프라이트 슬라이스"를 다시 돌려도 ID가 안 바뀐다
- 컴파일은 `dotnet build Assembly-CSharp-Editor.csproj`(에디터 밖)로 오류 0을 확인했다. **플레이 확인은 아직이다**

**눈으로 맞출 값** — 목업(방 바닥 + 캐릭터 + 판정 상자를 월드 단위로 합성)으로 고른 값이라 플레이에서 한 번 봐야 한다.
Q 2단은 판정 폭(18.9)에 가깝게 2.4로 두면 균열선 왼쪽 끝이 플레이어 몸을 덮어서 옛 그림 폭(15유닛)인 2로 뒀다.

**발견했지만 손대지 않은 것**

- **궁극기 시트 `.meta`가 ID 규칙을 안 따른다(0/6).** 09-13에 옛 `.meta`를 복사하며 spriteID만 새로 만들고 internalID는 옛 것을 뒀다.
  지금은 동작하지만 **"VFX 스프라이트 슬라이스"를 다시 돌리면 internalID가 spriteID 해시로 바뀌어 `KingsEmber`의 프레임 참조 7곳이 끊길 수 있다.**
  고치는 법: crown `.meta`의 spriteID 6개를 옛 `vfx_kings_ember_6frames_1536x256.png.meta`의 것으로 되돌리면 프리팹은 그대로다
- **대각선으로 걷다 멈추면 대각선 방향이 거의 안 남는다(코드 분석).** 방향키 둘을 같은 프레임(60fps에서 16.7ms 안)에 떼지 않으면
  먼저 뗀 키 때문에 한두 프레임 입력이 한 방향이 되고, `facingDirection`이 그걸로 덮인다. 상하좌우 한 방향은 항상 남는다.
  대기 방향·멈춘 채 쓰는 스킬 방향·입력 없는 대시 방향이 전부 영향을 받는다
- E 판정 반경 12(지름 24유닛)는 그림보다 훨씬 넓다. 새 그림은 기둥이 길어서 판정에 맞추면 기둥이 화면 두 배가 된다
- 활 발사 높이 `launchHeight 1.2`는 새 캐릭터의 활(가슴 높이)보다 낮다. 발사 섬광도 화살과 같은 높이에 맞춰서 같이 낮다(09-13부터의 확인 항목).
  **그냥 올리면 안 된다** — 일반 적의 몸 콜라이더가 발밑(높이 0~1.65)에만 있어서, 활 높이(약 2.7)로 올리면 같은 줄에 선 적 위로
  화살 콜라이더가 지나가 안 맞는다. 맞추려면 콜라이더는 그대로 두고 그림만 올리는 쪽이어야 한다

## 2026-09-15 — 일반 몬스터 3종 모션 교체

- **망령·사수·자폭병의 모션 시트 11장이 전부 새 그림이다.** 원본 `Art/NewMonsterImages/<몬스터>/`, 프롬프트 `_ArtReview/20260914-monster-v1/`,
  옛 시트 백업 `Sprites/Enemy/Raw/PreNewMonsterV1/`. 정규화 인자는 개발 로그 09-15 표에 있다
- 새 망령은 등 불꽃까지 키 165라 옛 망령(141)보다 크고 넓다. **몸 콜라이더(폭 2.9375유닛)는 아직 옛 크기다**
  → 갱신(09-15 밤): 빌더는 폭 4. 기존 프리팹은 인스펙터에서 바꿀 차례(아래 09-15 밤 항목)
- `AshSpriteSheetNormalizer` 표에 사수·자폭병 줄이 없다. 남은 적 줄은 사수 화살·자폭병 폭발 둘뿐이었다 → 09-16(`c4a1fa6`)에 둘 다 새 그림으로 교체 완료
  → 갱신(09-15 저녁): 두 줄도 뺐다. 표에 일반 적 줄이 없다
- **아직 옛 그림:** 사수 화살, 자폭병 폭발 VFX. 새 원본은 `NewMonsterImages`에 있다 → 갱신(09-15 저녁): 둘 다 교체했다(아래 항목)
- **자폭병 폭발 VFX는 판정과 어긋나 있다**(피벗이 바닥선이라 링이 약 3.1유닛 위에 뜨고 반지름도 약 3.1유닛, 판정은 발밑 중심 6유닛).
  새 폭발을 넣을 때 피벗을 가운데로, 배율을 약 2.0으로 맞춘다 → 갱신(09-15 저녁): 피벗 가운데, 배율은 새 그림을 재서 1.8
- **자폭 사망 모션**(`bomber_self_destruct_v1_raw.png`)은 생성만 됐다. 지금 코드는 자폭하면 몸을 숨기므로 연결이 필요하다
  → 갱신(09-15 밤): 연결했다(아래 09-15 밤 항목)
- 망령 VFX 2장(돌진 예고선·출발 자국)은 코드에 붙일 자리가 없다. 출발 자국은 프레임이 겹쳐 다시 뽑아야 한다
  → 갱신(09-15 저녁): 돌진 예고선은 붙였다. 출발 자국은 아직이다(다시 뽑기)
  → 갱신(09-15 밤): 출발 자국도 붙였다. 다시 뽑지 않고 덩어리 기준으로 프레임을 갈랐다
- **다음 작업(요청 추가): 방을 넘어갈 때마다 소품 배치 바꾸기**(위치 변경·빼기·추가). 지금 소품은 `AshDungeonPropPlacer`가
  `Game` 씬 `Props` 아래에 고정 위치로 한 번 깐 것이다. 소품에 충돌체가 있어 적 스폰 지점·문·출구·입장 위치를 피해야 한다

## 2026-09-15 (저녁) — 몬스터 VFX 교체와 망령 돌진 예고선

- **사수 화살·자폭병 폭발이 새 그림이다.** 옛 시트 백업 `Sprites/VFX/Raw/PreNewMonsterV1/`. 정규화 인자는 개발 로그 09-15 저녁 표에 있다
- **자폭병 폭발:** 피벗 가운데, 배율 1.8(링 바깥쪽 = 판정 반경 6). `AshBomber` 프리팹의 Explosion Effect Scale은 **디스크에 아직 1이다** — 인스펙터에서 1.8로 바꿔야 한다
- **망령 돌진 예고선:** 새 컴포넌트 `TelegraphLine` + 새 빌더 `AshWraithTelegraphBuilder`(메뉴 "망령 돌진 예고선 생성"). 예비동작 0.4초 동안 돌진 방향 바닥(Decal)에
  선을 긋고, 길이는 판정 상자가 닿는 끝이다(옆 돌진 12.27유닛). 유니티에서 슬라이스 → 빌더 실행 → **플레이 확인 전이다**
- **망령 돌진 판정 상자가 돌진 방향으로 안 돈다**(좌우 반전만). 위·아래 돌진에서 판정이 몸 옆으로 치우친다 — 보고만 하고 고치지 않았다
  → 갱신(09-15 밤): 고쳤다
- 남은 VFX: 망령 출발 자국(다시 뽑기), 자폭 사망 연결. 다 넣은 뒤 **파티클 곁들임 기획부터** 이야기한다
  → 갱신(09-15 밤): 둘 다 코드·시트까지 끝났다. 유니티 메뉴 실행과 플레이 확인이 남았다
- 유니티 시작 때의 경고 창은 씬 복구 알림이었다. `Assets/_Recovery/0.unity`는 `Title.unity`와 같은 내용이라 지워도 된다(사용자 확인 전)
- **상자·보스 보상 유물이 이제 아래(상자 앞)로 튄다**(`launchAngle` 270, 코드 기본값과 씬 저장값 모두). 위로 튀면 Y축 정렬 때문에 상자 뒤에 가려졌다
- **렉 조사 중:** 타이틀 → 튜토리얼은 동기 `LoadScene`(첫 로드 4.5초, 두 번째 0.55초 — USB 외장 드라이브의 첫 읽기가 대부분).
  보스 방 입장은 방 스크립트가 아니다(5ms) — `조사 → 프로파일러 스파이크 보고서`로 스파이크 내역을 받아 원인을 가린다. 최적화는 원인 확인 후 사용자 결정

## 2026-09-15 (밤) — 몬스터 버그 수정과 VFX 마무리

**고친 버그**

- **망령 돌진 판정이 돌진 방향으로 돈다.** 예비동작에서 방향을 고정할 때 `EnemyWraith.OrientChargeHitbox`가 판정 상자를
  **예고선 출발점(발밑 위 0.705) 기준**으로 돌린다. 자식 원점(발밑)을 축으로 돌리면 세로 오프셋까지 돌아서 위로 돌진할 때 상자가 선 옆으로 0.7유닛 비켜난다.
  좌우 반전(`OnFacingChanged`)은 이제 상자를 안 건드린다. 선 길이는 어느 방향이든 12.27유닛이다
- **예고선이 판정 굵기만큼 두꺼워진다.** `TelegraphLine.Show`에 굵기 인자가 생겼고, 그림 굵기 `drawnThickness`(여섯 프레임 세로 범위의 가운데 값 1.875유닛)로
  판정 폭 2.94유닛에 맞춰 세로를 늘린다(1.57배). 길이와는 여전히 무관하다
- **점화 중인 자폭병이 맞아도 점화 그림이 안 끊긴다.** `EnemyBase.CanBeStaggered`(자식이 정함)가 거짓이면 넉백·피격 트리거·`OnStaggered`를 건너뛴다. 대상 잡기는 그대로 한다
- **망령 몸 콜라이더 폭 4**(빌더 `AshEnemyPrefabBuilder.BodyWidth`). 목업에서 뒷다리~앞발을 덮는 폭이다. 사수·자폭병은 새 그림도 옛 폭(2.94) 안에 발이 들어와서 그대로 뒀다

**VFX 마무리**

- **자폭 사망:** 시트 `Sprites/Enemy/ash_bomber_self_destruct_6frames_1536x256.png`, 구간 `self_destruct`는 **1번 칸부터 5칸 / 8fps**(0번 칸은 점화 마지막 칸과 같은 그림).
  스스로 터져 죽을 때 `EnemyBase.DeathTriggerHash`로 `SelfDestruct` 트리거를 건다. 애니메이션 빌더는 이 파라미터를 **클립이 있을 때만** 선언하고,
  `EnemyBomber`는 파라미터가 없으면(메뉴를 아직 안 돌렸으면) 예전처럼 그림을 감춘다
- **망령 출발 자국:** 시트 `Sprites/VFX/vfx_wraith_charge_launch_6frames_1536x256.png`, 슬라이서 `Forward`, 프리팹 `WraithChargeLaunch`(16fps, 끝나면 지움, Decal 1, 배율 1).
  `EnemyWraith.BeginCharge`에서 출발 자리 발밑에 돌진 방향으로 놓는다. 메뉴는 `망령 돌진 출발 자국 생성`(예고선 빌더와 같은 클래스)
- 원본을 다시 뽑지 않았다. `NormalizeVfxStrip.ps1 -SeparateBlobs`(새 옵션)가 이어진 픽셀 덩어리로 프레임을 갈라 벌려 놓는다.
  `NormalizeGeneratedStrip.ps1 -GreenKey`(새 옵션)가 폭발과 섞인 올리브 가장자리를 배경으로 친다. 두 옵션 모두 끄면 이전 결과와 같다(점화 시트로 0px 차이 확인)
- 컴파일(`dotnet build`) 오류 0·경고 0. **유니티 메뉴 실행과 플레이 확인은 아직이다**

**유니티에서 할 일 (순서대로)**

1. `VFX 스프라이트 슬라이스` → `캐릭터 스프라이트 슬라이스` → `캐릭터 애니메이션 생성` → `망령 돌진 예고선 생성` → `망령 돌진 출발 자국 생성`
2. 인스펙터: `AshEmberWraith`의 Capsule Collider 2D **Size X 4**, `AshBomber`의 Enemy Bomber **Explosion Effect Scale 1.8**
3. `접지 그림자 추가`(망령 그림자가 새 콜라이더 폭을 따라간다)

**발견했지만 손대지 않은 것**

- **자폭병 자기 사망이 무적 시간에 막힐 수 있다.** `EnemyBomber.Explode`가 `Health.TakeDamage`로 자기를 죽이는데, 점화 끝나기 0.35초 안에 맞고 살아남았으면
  피격 무적이라 무시된다. 상태는 Dead라 멈춘 채 안 죽고, 방이 안 끝난다. 지금은 자폭병 체력 2가 모든 공격 피해 이하라 안 나온다.
  체력을 올려 점화 중 피격을 시험하면 바로 나온다
  → 갱신(09-16): 고쳤다. `Health.Kill()`(무적을 무시하고 확정 사망 — 피격 연출 없이 Changed·Died만 알린다)을 만들고
  `EnemyBomber.Explode`가 그것을 쓴다. 방 집계·풀 복귀는 사망 이벤트를 그대로 타고 이어진다

## 2026-09-16 — 무적 상대에게 나가던 타격감

- **`DamageHitbox`가 `TakeDamage`의 결과를 보고 막혔으면 거기서 끝낸다.** 예전에는 결과를 버려서 무적으로 막힌 공격도
  히트스톱·카메라 흔들림·`HitLanded`(화살 명중 불꽃)를 그대로 냈다 — 대시로 흘려도 맞은 것처럼 멈췄고, 피격 직후
  0.35초 무적인 적이나 전환 중인 보스를 쳐도 같았다. 주석은 원래부터 "걸러지면 여기까지 안 온다"였는데 코드가 그러지 않았다
- 막힌 대상도 `damagedThisActivation`에는 남긴다 — 한 번의 공격은 같은 대상에게 한 번만 기회를 갖는다.
  빼면 판정이 켜져 있는 동안(돌진 0.34초) 나갔다 들어올 때 같은 공격에 두 번 걸린다
- **회피 보상의 자리이기도 하다.** 조건형 유물 "스친 불씨"(흘리면 재 게이지)를 넣는다면 이 거짓 반환이 그 신호다
- 자폭병 쪽 수정(`Health.Kill()`)을 코드로 검증했다 — `Damaged`를 안 쏘지만 그 이벤트의 구독자 넷(적 공통·보스·플레이어·허수아비)은
  전부 체력 0에서 아무 일도 안 하므로 빠진 것이 없다. 처치 집계·재 게이지는 사망이 아니라 **풀 복귀**(`DespawnRequested`)에 붙어 있어
  그대로 이어진다. `Kill()` 문서에 "스스로 끝내는 경우에만 쓴다"는 제한을 적어 뒀다
  → 수정 반영: `Health.Kill()`로 무적과 관계없이 사망 이벤트를 발생시킨다. 기존 사망 연출 후 스포너가 적 목록에서 제거하고 풀로 반환하며, 마지막 적이면 방을 완료한다. 플레이 재현 검증은 남았다.
- 망령 출발 자국의 세로 방향 타원 모양은 사용자 요청으로 그대로 둔다.

## 2026-09-17 — 플레이어 파티클 곁들임 14개

- **기획은 09-16 페이지에서 사용자가 골랐다.** 공격-1·2, 대시-1, W-1·3, Q-1·2·3, E-1·2·3, R-1·2·3(대시-2·W-2 뺌),
  양은 "화려하게"(목업 개수 ×1.5), E 판정은 그림에 맞춘다. 기획 페이지: https://claude.ai/artifact/BBQzCoYwYUJyB8QDzqfVkU
- **대부분 기존 이펙트 프리팹의 자식으로 넣었다**(스킬이 정한 자리·방향·시각을 그대로 따라간다).
  이펙트는 그림이 끝나면 스스로 지워지므로, `ParticleGarnish`가 붙은 자식을 지우기 직전에 월드로 떼어 낸다
  (`SpriteFrameAnimator` 끝, `Projectile` 사거리 끝). 떼어진 파티클은 Stop Action(Destroy)으로 스스로 사라진다
- `ParticleGarnish`는 표식도 겸한다. 이펙트를 복제해 새 이펙트를 굽는 빌더 다섯(참격·자폭병 폭발·보스 전환·사수 화살·잿불 파도)이
  복제 직후 곁들임을 걷어낸다 — 안 그러면 KingsEmber를 복제한 자폭병 폭발에 R의 재 비가 딸려 간다
- 독립 프리팹은 둘: `Prefabs/VFX/Particles/HitSparks`(공격-2, 검 판정의 `HitSparkSpawner`가 `DamageHitbox.HitLandedAt`을 듣고 만든다),
  `UltimateGather`(R-1, `AreaSkillData.castEffectPrefab` — 시전 순간 몸에 만든다)
- 대시-1은 플레이어 자식 `DashTrail`(Decal 층, 거리 기준 방출)이고 `PlayerController`가 대시 동안만 방출을 켠다
- Q는 그림을 flipX·가로 배율로 맞추므로 `ParticleGarnish.MatchSprite`가 곁들임을 Y축 180도 회전(좌우 반전)과 셰이프 가로 배율로 따라가게 한다
- R-2 고리는 `AreaRingParticles`가 스킬의 반경으로 시작 속도를 다시 계산한다(속도 배율 곡선이 수명 35%에서 0)
- **E 판정을 그림에 맞췄다** — `AreaSkillData`에 `verticalScale`(판정 원을 세로로 누름)과 `areaCenterOffset`(판정 중심 이동)을 더했다.
  E는 반경 12 → 4.5, 세로 0.66, 중심 +0.9(고리 그림의 중심). R은 기본값(1, 0)이라 판정이 그대로다
- 공통 규칙: 텍스처 없는 Sprites-Default(네모 조각), 월드 공간, Scaling Mode Local(부모 배율이 크기에 안 먹음), VFX 층(바닥에 남는 것만 Decal)

### 유니티에서 할 일 (09-17)

1. 스크립트 컴파일이 끝난 뒤 **Tools → 재의 길 → 파티클 → 플레이어 파티클 만들기** 한 번. 콘솔에 만든 것 목록이 나온다
   (이미 있는 자식은 안 건드린다 — 처음 값으로 되돌리려면 그 자식을 지우고 다시 돌린다)
2. 플레이 확인 — 콘솔에 확인할 것 네 가지가 같이 찍힌다
   - 기본 공격 명중 불똥이 적 몸통 높이에서 공격 방향으로 튀는가(아니면 `Player/AttackHitbox` → Hit Spark Spawner → Spark Height)
   - 참격 불티가 초승달 바깥 호에서 나오는가(호가 반대편이면 `EmberSlash/SlashEmbers` Shape Rotation 부호)
   - Q를 좌우로 써서 번지는 불티가 몸에서 먼 쪽으로 번지는가, 균열 길이 안에서만 솟는가(`SlamBurst/SlamRisingEmbers` Shape Radius·Speed)
   - R 불티 고리가 판정 경계(반경 14)에서 멈추는가, E 고리 밖(예전 반경 12 안)의 적이 안 맞는가
3. `Player.prefab`을 **플레이어 프리팹 생성** 메뉴로 다시 구우면 HeroLight·대시 자국과 함께 대시 불씨·명중 불똥도 빠진다. 그때는 1을 다시 돌린다

### Q 판정 정리 (09-17, 사용자 지시)

- **Q 히트스톱은 2단에서만 난다**(사용자 결정). 1단과 2단이 0.17초 차이라 1단에서도 멈추면 끊긴 두 방으로 읽혔다
- **Q 판정 상자가 그림을 따라 돈다.** 그림은 위·아래로 칠 때 90도 돌아 세로 균열(불길은 위로 칠 때 왼쪽, 아래로 칠 때 오른쪽)이 되는데
  판정은 가로 띠 그대로였다(위로 칠 때 2단 그림 x -10.1~0.9 · y 4.0~12.2, 판정 x ±9.45 · y 8.1~16.5).
  이제 `GroundSlamSkillData.PoseFor`가 구한 반전·회전·원근 길이를 그림과 판정이 같이 쓴다 — 상자 길이에 원근 배율, 상자 중심과 상자 자체를 같은 각도로 돌린다.
  좌우로 칠 때는 예전과 같은 상자다. 위·아래로 칠 때는 불길이 그려진 쪽만 맞는다(그림이 한 종류라 생기는 결과 — 불편하면 다시 정한다)

- **Q 2단이 붙어 있는 적에게 들어간다**(사용자가 방법 1 선택). 원인은 히트스톱이 아니라 `Health`의 피격 무적(맞을 때마다 0.35초)이었다 —
  1단(0.25초)이 무적을 걸면 0.17초 뒤의 2단이 늘 막혀서, 붙은 적은 2, 먼 적은 5를 받았다(주석 의도는 7).
  `Health.TakeDamage(amount, source, grantInvulnerability)`를 더하고 Q 1단만 false를 넘긴다.
  이미 걸린 무적(대시·다른 공격의 피격 무적)은 그대로 막는다 — 무적을 무시하는 게 아니라 1단이 새로 걸지 않을 뿐이다
- **Q·E·R 히트스톱은 데미지가 실제로 들어간 대상이 있을 때만 난다**(사용자가 방법 1 선택). `GroundSlamSkillData.ApplyDamage`와
  `AreaSkillData.Execute`가 `TakeDamage` 결과를 보고, 한 대상이라도 맞았을 때 한 번 멈춘다(09-16 `DamageHitbox`와 같은 규칙).
  Q는 여기에 "2단에서만"이 겹친다

### 사수 화살 높이와 몬스터 파티클 결정 (09-17 밤)

- **사수 화살은 그림만 활 높이(4.5)로 띄운다.** 판정은 발치(launchHeight 1)에 그대로 둬야 같은 줄의 플레이어를 맞힌다.
  `Projectile.visual`·`SetVisualLift`, `EnemyMarksman.bowHeight`. 유니티에서 **잿불 사수 화살 프리팹 생성** 메뉴를 다시 돌려야 그림이 자식으로 옮겨진다
- **몬스터 파티클 결정**(페이지 https://claude.ai/artifact/LrUxgRgfHMKVYxmxRjLeAi): 망령-1·2, 사수-1·2·3, 자폭병-1·2·3·4, 공통-1, 양 화려하게, 적 불티는 흰 심 없이.
  구현은 09-18. `EnemyBase.deathEffectPrefab`과 `EnemyBomber` 점화 파티클 코드는 먼저 들어가 있다(프리팹 연결 전이라 동작 없음)
  → 갱신(09-19): 10개 코드와 빌더(`Tools → 재의 길 → 파티클 → 몬스터 파티클 만들기`, `AshMonsterParticleBuilder`)까지 끝. 유니티에서 메뉴를 한 번 돌리면 연결된다.
  적 프리팹을 적 프리팹 빌더로 새로 구우면 붙인 자식·참조가 빠지므로 이 메뉴를 다시 돌린다

## 오늘 완료한 작업 — 2026-09-04

**인벤토리 화면이 두 벌이라 게임이 멈추던 버그**

- `Game.unity`에 `InventoryScreen`이 둘 있었습니다. `SettingsCanvas` 아래(폰트 Regular96,
  한글 정상)와 `GameHUD` 아래(폰트 Regular32, 한글이 두부로 나오던 옛것)입니다
- **증상**: `I` → `T` → `T` 를 누르면 시간이 멈춘 채 돌아오지 않습니다. 둘 다 `I`를 듣고
  각자 `PauseGate`에 들어가는데, 보스 열쇠 화면은 `FindFirstObjectByType`으로 **하나만**
  찾아 닫습니다. 남은 하나가 스택에서 안 빠져 `timeScale`이 0에 고정되고, 그 뒤로 `I`는
  두 화면을 번갈아 켜기만 해서 스택이 절대 안 빕니다
- **원인**: `AshInventoryUiBuilder.FindOrCreateCanvas()`가 `FindObjectsSortMode.None`으로
  캔버스를 골랐습니다. 이름 그대로 순서를 보장하지 않는 모드라, 다시 실행할 때 지난번과
  다른 캔버스가 집히면 `canvas.transform.Find("InventoryScreen")`이 지난 것을 못 찾습니다.
  한글 폰트를 고치며 빌더를 다시 돌린 그 실행에서 한 벌이 더 생겼습니다
- **수정**: 중복 제거를 캔버스 기준이 아니라 **컴포넌트 타입으로 씬 전체**를 훑도록 바꿨습니다.
  캔버스 선택도 ① 이미 화면이 올라가 있는 캔버스 ② 없으면 이름순 첫 번째로 못 박았습니다
- `AshBossKeyUiBuilder`도 **같은 구조의 같은 버그**였습니다. 아직 안 터졌을 뿐이라 같이 고쳤습니다
- 빌더 수정은 도구를 다시 돌릴 때만 듣습니다. 씬을 손으로 복사해도 잡히도록
  `InventoryScreen`·`BossKeyScreen`의 `Awake`에 개수 검사를 넣었습니다(Awake 1회, 프레임 비용 없음)

**보스 2페이즈 전환 — 시그널 유실 안전망**

- `EnterPhase2`가 타임라인 길이만큼 기다린 뒤 **페이즈가 실제로 바뀌었는지 확인하지 않았습니다**
- 시그널이 안 오면 무적만 풀려서, 1페이즈 컨트롤러를 문 채 스프라이트가 꺼져 있고 맞기만 하는
  보스가 됩니다. `transitionStarted`가 이미 참이라 다시 시도하지도 않습니다
- 대기 뒤에 `if (!isPhase2)` 검사를 두고, `OnTransitionRevealed`·`OnTransitionBossReturns`를
  각각 한 번만 돌도록 막았습니다. 시그널이 절반만 도착해도 **안 온 쪽만** 채워집니다
- 지금 배선(SignalReceiver + INotificationReceiver 이중 수신)은 정상이라 당장 터지지는 않습니다

**키 바인딩을 한 번 뺏기면 영영 못 되돌리던 버그**

- **증상**: Q 스킬의 Q를 인벤토리로 옮기면 Q 스킬에서 Q가 빠지는 것까지는 정상인데,
  그 뒤로 Q 스킬을 **어떤 키로도 다시 정할 수 없습니다.** 원래 키인 Q로 되돌리는 것조차
  안 되고, 설정 초기화만이 탈출구였습니다
- **원인**: `ClearConflicts`가 겹친 키를 빈 문자열로 덮습니다. 그러면 그 자리의
  `effectivePath`가 비는데, `FirstKeyboardBindingIndex`가 빈 경로를 건너뛰어 `-1`을 냅니다.
  `StartRebind`는 `-1`이면 "바꿀 키보드 바인딩이 없다"며 거절합니다
- **스킬에서만 터진 이유는 키보드 자리 개수**입니다. 대시(Shift 좌우)·기본 공격(Ctrl 좌우)·
  인벤토리(I/Tab)는 자리가 둘이라 하나가 비어도 나머지가 잡힙니다.
  스킬 Q·W·E·R은 자리가 하나뿐이라 비는 순간 `-1`이 됩니다
- **수정**: 자리를 `effectivePath`가 아니라 `path`(원래 설계된 경로)로 찾습니다. 필요한 것은
  "지금 무슨 키인가"가 아니라 **"어느 자리에 넣을 것인가"**라 덮어쓰기와 무관해야 합니다
- `ShortKeyTextFor`에는 빈 자리를 `-`로 보이게 하는 검사를 더했습니다. 안 그러면 스킬바에
  아무것도 안 적힌 칸이 나옵니다

**정리**

- `Assets/_Recovery/0.unity`(813KB) — 유니티 크래시 복구 씬이 `Assets/` 안에 있어 빼냈습니다

## 오늘 조사만 하고 손대지 않은 것 — 2026-09-04

**텍스처 메모리 260MB, 그중 254MB가 무압축**

씬·프리팹이 참조하는 텍스처 88장을 전수 계산한 결과입니다.

| 항목 | 값 |
| --- | --- |
| 참조 텍스처 총합 | 260 MB |
| 압축이 실제로 먹는 것 | -144 MB (69장) |
| 가로세로가 4의 배수가 아니라 막힌 것 | 46 MB (16장) |
| 압축 후 예상 | 116 MB |

덩어리의 절반이 **플레이어 시트 10장(124MB)**입니다. `player_walk` 2048×2048 한 장이 16MB입니다.

압축을 켜려 두 번 시도했으나 둘 다 되돌아왔습니다. **원인은 임포트 설정이 아니라 코드입니다** —
`AshSpriteImportRules`(AssetPostprocessor)가 임포트할 때마다 캐릭터 시트와 UI를
`TextureImporterCompression.Uncompressed`로 되돌립니다(257·281줄).

의도적으로 넣은 규칙이라 손대지 않았습니다. 다만 주석의 근거 한 줄은 지금 사실과 다릅니다 —
"캐릭터 텍스처는 몇 장뿐이라 무압축이어도 용량 부담이 없다"고 적혀 있는데 실제로는 124MB입니다.
그리고 주석이 겨냥한 것은 **DXT5(BC3)**인데, BC7은 메모리는 같은 1바이트/픽셀이면서 블록당
색 엔드포인트가 훨씬 많아 1~2px 대비가 살아남습니다. 그때의 판단이 BC7에 그대로 적용되지는 않습니다.

**플레이 영상 분석(5분 50초, 10,520프레임 전수)**

- **지속적인 프레임 저하는 없습니다.** 전투 중 150프레임당 133~142개가 변합니다(89~95%)
- **347.0초에 1.9초 완전 정지** — 사망 → 결과 화면 → `R` 재시작의 **씬 로드**입니다.
  260MB를 다시 올리는 지점이고, `Game.unity`에 방 4개가 전부 상주해 재시작마다 반복됩니다
- **58~81초에 133~233ms 히치 12건** — 설정·인벤토리·보스 열쇠 패널을 여닫는 순간마다 붙습니다.
  37MB짜리 4096×4096 한글 폰트 아틀라스의 첫 GPU 업로드로 보입니다
- 85초 이후 275초까지는 히치가 거의 없습니다. **첫 사용 워밍업 패턴**입니다

**보스 2페이즈 전환은 영상에서 정상 확인**했습니다. 갑옷 붕괴 → 재 파티클 → 알 응축 →
껍질 깨짐 → 2페이즈 등장, 이름 `???` → `재의 길` 교체와 바 재충전까지 계획표대로입니다.

## 지금 가는 방향 — 2026-09-09 확정

목표는 **"서브컬쳐 게임의 느낌"** 인데, 그것을 아트 문제가 아니라 **연출 시스템 문제**로
번역해서 갑니다. 캐릭터 디자인(치비·흰머리·붉은 눈, 보스의 갑옷 → 진체 폼 체인지)은 이미
그 문법이고, 부족한 것은 그 캐릭터를 주인공으로 보여주는 연출입니다. 그리고 그쪽은 전부
프로그래머의 몫이라 포트폴리오로도 남습니다.

| 순서 | 할 것 | 비고 |
| --- | --- | --- |
| 1 | **히트스톱 + 카메라 반응** | `CameraShake`가 이미 있음. `PauseGate`가 소유하게 설계 |
| 2 | **데미지 팝업** | 풀링 구조를 겸함 — 투사체 풀링이 미완이라 같은 뼈대를 씀 |
| 3 | **컷인 시스템** | 궁극기·보스 등장. 이미지는 마지막에 끼움 |
| 4 | **이미지 퀄리티 올리기** | 생성 그림의 품질을 올릴 수 있으면 시도 |
| — | 사운드 | **맨 마지막.** 다 만든 뒤에 넣기로 함 |

2026-09-14: 보스 기믹(못 막으면 궁극기 컷인 → 빈사/즉사) 기획안을 `Docs/BOSS_GIMMICK_PLAN.md`에 저장했다. 결정 대기, 일반 몬스터 교체 다음 작업.

**히트스톱은 `PauseGate`가 소유해야 합니다.** 문서에 "`PauseGate`가 `Time.timeScale`을
만지는 유일한 곳"이라고 못 박혀 있는데, 히트스톱은 두 번째 주체가 됩니다. 디버그 배속(F7~F9)이
이미 "활성일 때 매 프레임 다시 먹이는" 방식으로 우회하고 있어서, 셋이 되면 누가 이겼는지
추적이 안 됩니다.

**사운드를 나중에 넣으면 히트스톱 타이밍을 다시 만지게 됩니다.** 정지 길이를 "얼마나 멈춘 것처럼
보이는가"로 맞추는데, 소리가 붙으면 기준이 "소리와 정지가 같이 터지는가"로 바뀝니다.

스탯 표기는 보류입니다. 하게 되면 **인벤토리에 탭으로** 붙입니다(`SettingsScreen`의 탭 구조를
복사). 별도 화면으로 만들면 장착 슬롯 3칸이 화면에서 사라져서 **"유물을 빼면 숫자가 준다"를
볼 수 없습니다.** 그게 이 화면의 전부인데 말입니다. 인벤토리 패널이 꽉 차 있어 나란히 붙일
자리는 없습니다.

## 다음에 고칠 것 — 우선순위 순

### 1. ~~텍스처 압축~~ → 2026-09-06 적용, 화질 확인 남음

`AshSpriteImportRules`의 캐릭터 규칙을 `Uncompressed` → **`CompressedHQ`(BC7)**로 바꾸고
`compressionQuality`를 100으로 뒀습니다(임포트 시간에만 영향, 런타임과 무관).

**같이 넣은 것: `GetVersion()`.** 이게 없으면 png가 안 바뀌었다는 이유로 캐시된 임포트
결과가 재사용되어 **규칙을 고쳐도 아무 변화가 없습니다.** 앞으로 이 파일의 규칙(PPU, 필터,
압축)을 바꿀 때마다 그 숫자를 1 올려야 합니다.

감사 결과 **BC7 164장 96.5MB**로 실제 적용을 확인했습니다(`Texture2D.format` 직접 확인).

**남은 것은 눈으로 보는 것뿐입니다.** 후드 그림자와 검날 균열의 1~2px 대비가 살아 있는지
확인하고, 별로면 `AshSpriteImportRules`의 그 한 줄만 `Uncompressed`로 되돌립니다.
`.png.meta`는 git이 추적하므로 `git checkout -- "Assets/Project/Art"`로 원복됩니다.

### 2. ~~궁극기 캐릭터 모션이 VFX에 가려집니다~~ → 09-06 해결(`8077f8a`, castDelay 0.3 → 0.5)

모션 자체는 정상입니다 — `Ultimate_N.anim`은 6프레임/10fps/**0.6초**이고
`Skill_R_KingsEmber.motionSeconds`도 0.6으로 일치합니다. 애니메이터 배선도 다른 스킬과 같습니다.

문제는 이펙트가 캐릭터 **위**에 그려지고 화면을 덮는 크기라는 점입니다.

| | Sorting Layer | 크기 |
| --- | --- | --- |
| Player | 4 | — |
| `KingsEmber` 이펙트 | **5** (위) | 8×8 유닛 × scale 4.5 = 약 36유닛 |

`castDelay: 0.3`이라 0.6초 모션의 **딱 절반**에서 덮칩니다. 뒷부분 3프레임이 안 보입니다.

**→ 2026-09-06 해결. `castDelay` 0.3 → 0.5.**

레이어와 배율은 답이 아니었습니다. VFX 프리팹 **12개가 전부** Sorting Layer 5(VFX)이므로
Entity 위에 그리는 것은 프로젝트 공통 규칙이지 이 프리팹의 실수가 아니고, 크기도 판정
반경 14(지름 28유닛)를 그리고 있는 것이라 줄이면 **보이는 것보다 넓게 맞는** 상태가 됩니다.

남는 것은 시점입니다. 0.5면 6프레임 중 5프레임이 보이고 마지막 프레임에서 겹칩니다.
`explodeDelay: 0.15`라 판정은 0.65초 — 모션이 끝난 뒤 터지는 순서가 되어 인과가 분명해집니다.

`AreaSkillData`에 규칙으로 적어뒀습니다: **이펙트가 시전자를 덮는 스킬이면 동작 프레임만으로는
부족하고 모션이 거의 끝난 뒤여야 한다.** 이펙트가 몸에서 떨어져 나가는 E(`forwardDistance 8`)
에는 해당하지 않습니다.

### 3. ~~결과 화면 줄바꿈이 깨집니다~~ → 09-24 해결(제목 줄바꿈 끔)

"재가 되었다"가 `재가 되었` / `다` 로 잘립니다. 마지막 한 글자만 다음 줄로 넘어갑니다.
제목 텍스트 박스 폭 문제입니다.

### 4. ~~이미지 크기를 4의 배수로 (-46MB)~~ → 09-24 정리. 실제 대상은 2장뿐이었다(아래 갱신 참고)

압축을 켜도 가로세로가 4의 배수가 아니면 BC 계열은 **경고 없이** 무압축으로 되돌아갑니다.
인스펙터에는 "Compressed"로 보이기 때문에 눈으로는 절대 안 잡힙니다.

| 크기 | 장수 | 해당 |
| --- | --- | --- |
| 1254×1254 | 다수 | 유물 아이콘 전부, 던전 프롭, 보스 열쇠 패널 |
| 1678×937 | 3 | 방 배경 |
| 1672×941 | 2 | `Title.png`, `GameClear.png` |
| 1520×931 | 1 | `InventoryPanel.png` |

1254 → 1252 처럼 1~2픽셀만 줄이면 됩니다.

**2026-09-06 추가 — 이 목록에 캐릭터 시트가 하나 있습니다.**
`Assets/Project/Art/Sprites/Boss/king_of_ashes_sprite_sheet.png`가 1254×1254라 BC7 설정을
받고도 **무압축으로 남았습니다.** 나머지 캐릭터 시트(1536×2048, 2048×2048, 1536×256,
1024×256)는 전부 통과했습니다. `텍스처 포맷 감사`로 언제든 다시 확인할 수 있습니다.

### 5. ~~한글 폰트 아틀라스 37MB가 시작부터 상주~~ → 09-24 도구 추가(`그림 → 한글 폰트를 Dynamic으로`), 유니티에서 실행 필요

`NeoDunggeunmoPro-Regular96.asset`은 4096×4096 **정적** 아틀라스에 완성형 한글 11,172자를
전부 구워둡니다. TMP Settings의 Fallback에 등록돼 있어 게임 시작 시 올라가고 안 내려갑니다.
실제로 화면에 나오는 한글은 수백 자 수준이라 `AtlasPopulationMode`를 **Dynamic**으로 바꾸면
쓰는 글자만 굽습니다.

### 6. 스프라이트 아틀라스가 하나도 없습니다

`.spriteatlas` 파일이 없어 스프라이트가 전부 개별 텍스처입니다. 배칭이 안 되고
드로우콜이 화면의 스프라이트 수만큼 나갑니다.

### 7. 투사체·이펙트 풀링

적은 `ObjectPool<EnemyBase>`로 잘 풀링돼 있는데 아래는 매번 `Instantiate` + `Destroy`입니다.
보스 2페이즈처럼 투사체가 쏟아지는 구간에서 GC 스파이크로 나타납니다.

`Projectile`, `EnemyBoss.wavePrefab`, `EnemyBoss.ultimateEffectPrefab`,
`EnemyMarksman.arrowPrefab`, `ProjectileSkillData`, `SkillData`, `EnemyBomber`

### 8. ~~보스 이름이 "재의 길"~~ → 의도 확정 (2026-09-06)

게임 제목과 같습니다. 프리팹은 `BossAshKing`이고 코드 주석은 "재의 왕"이라 부르는데
체력바 2페이즈 이름만 "재의 길"입니다. **의도가 맞다고 확인받았습니다 — 건드리지 않습니다.**

고치게 될 경우를 위해: `BossHealthBar.cs`의 기본값만 바꾸면 안 됩니다.
`Game.unity`에 이미 직렬화돼 있어서 씬 값을 같이 바꿔야 합니다.

### 9. 남은 정리

- `RoomSequenceController`의 `#if UNITY_EDITOR` 계측 블록 — 방 입장 렉의 원인을 잡은 뒤 지웁니다.
  아직 남겨둔 이유는 활성화/초기화/전투시작 구간을 나눠 찍어주기 때문입니다
- `Raw/`·`Pre*` 백업 136MB(129장)가 `Assets/` 안에 있습니다. **전수 확인 결과 씬·프리팹·
  애니메이션 어디서도 참조되지 않아** 빌드와 런타임 메모리에는 영향이 없지만,
  프로젝트 열기·리임포트 시간과 Library·git 용량에 그대로 들어갑니다

## 오늘 완료한 작업 — 2026-09-01

**밀린 커밋 분리**

- 08-28 이후 커밋이 0건이었습니다. 설정 창·입력 중앙화·사수·자폭병·정규화 도구 수정이
  한 덩어리로 쌓여 있던 것을 10개로 나눴습니다
- 씬 두 개는 위 작업이 한 파일에 겹쳐 들어가 나눌 수 없어 따로 묶었습니다

**보스 체력바 HUD와 이름표**

- `BossHealthBar` — 페이즈별 채움, 전환 시 재충전, 이름표, 나타남/사라짐
- `BossEncounter`가 보스 생성 직후 `Bind`, 방 정리 때 `Unbind`
- 이름 `???` → `재의 길`, 교체는 `MarkPhase2()`
- 보스 바 아트 2장을 `AshGaugeArtProcessor`에 넣어 초록 배경을 알파로 바꿨습니다

**HUD 배치를 코드로 되돌림**

- 씬에서 손으로 맞춘 네 값(체력 954 / 스태미나 884 / 재 게이지 808 / 보스 바 600×118.3, -28)을
  빌더 상수에 넣었습니다. 전체 HUD 생성을 눌러도 지금 배치가 그대로 나옵니다
- `보스 체력바만 생성 (나머지 HUD 유지)` 메뉴 추가 — 전체 생성이 HUD를 통째로 지우기 때문입니다

**버그 2건**

- 게이지 다듬기 도구가 안쪽 영역의 **위아래를 뒤집어 보고**했습니다. `GetPixels32()`가
  아래쪽 줄부터 담는 것을 놓친, 어제 정규화 도구에서 고친 것과 **같은 실수**입니다.
  여태 안 걸린 이유는 프레임들의 위아래 여백이 대칭이었기 때문입니다(52/51)
- **죽는 한 대가 페이즈 전환을 켜고 갔습니다.** `Damaged → Changed → Died` 순서라 죽인 한 대도
  `OnDamaged`를 먼저 거칩니다. 현재 수치로는 안 나오지만 **보스 체력을 낮춰 테스트하면 바로**
  나옵니다

**2페이즈 전환 연출 기획**

- 재가 화면을 덮음 → 원형으로 모여듦 → 알 → 껍질이 깨지며 2페이즈 등장
- 시트 3장(`gather`/`egg`/`shatter`)을 뽑아 정규화 표에 등록했습니다. 아직 `_raw`만 있습니다

**확인 못 한 것: 유니티 컴파일.** 오늘 만진 코드가 전부 에디터를 안 거쳤습니다.

## 알려진 미해결 — 그림 재생성이 필요한 것

아래 4개 방향은 어두운 픽셀이 원본에서부터 빠져 있어 도구로 복원할 수 없습니다.
대칭 짝이 없거나(N) 짝도 손상된 경우입니다.

| 시트 | 방향 | 보이는 상황 |
| --- | --- | --- |
| `player_walk` | N | 위로 걸을 때 |
| `player_dash_hit` | N | 위로 대쉬할 때 |
| `player_ultimate` | SW, SE | 좌하·우하 궁극기 |

2026-09-13: `player_dash_hit`와 `player_ultimate`는 새 캐릭터로 다시 뽑아 해결. `player_walk`도 09-10에 새 시트로 바뀌어 위 표는 옛 시트 기준이다.

다시 뽑을 때는 **셀을 키우거나 참격·잔상을 별도 VFX로 분리**합니다. 256px 셀에 캐릭터와
큰 궤적을 같이 그리면 칸을 넘어가고, 그게 지금까지 조각이 섞인 원인입니다.
Q 스킬은 이미 이펙트를 프리팹으로 분리한 구조라 그대로 참고하면 됩니다.

## 내일 할 일 — 2026-09-02

### 0. 먼저 — 컴파일과 도구 실행

오늘 코드가 에디터를 안 거쳤습니다. 콘솔부터 확인하고, 전환 시트 3장을
`원본 시트 정규화 (고른 것만)`으로 돌립니다(전체를 돌리면 보스·플레이어 시트까지 바뀝니다).

### 1. 2페이즈 전환 연출 — 보스 마무리

8fps 기준 총 3.125초. `1.625~2.375`의 알이 맥동하는 구간에 **일부러 정적을 둡니다** —
흔들고 터뜨리기만 하면 소음이 되고, 그 0.75초가 있어야 깨지는 순간이 삽니다.

| 시각 | 보스 | 파티클 | 시트 | 화면·UI |
| --- | --- | --- | --- | --- |
| `0.000` | 갑옷 붕괴, 무적 ON | 장막 방출 시작 | — | 흔들림 강, 바 탈색 |
| `0.875` | 스프라이트 숨김 | 방출 정지 + 힘의 장 ON | `gather` | 화면의 재가 발밑으로 빨려듦 |
| `1.625` | — | 궤도 잔재만 | `egg` | 정적 |
| `2.375` | — | 파편 버스트 | `shatter` | 플래시 + 이름 교체 + 바 재충전 |
| `2.875` | 2P 스프라이트 등장 | — | 실루엣과 겹침 | — |
| `3.125` | 무적 OFF | 잔재 소멸 | 정리 | 흔들림 종료 |

만들 것:

- `BossTransitionSequence` — **연출의 시간표를 이 스크립트가 소유합니다.** 보스는 "시작"만
  알리고 "끝났다"를 받습니다. `transitionSeconds`를 두 곳에 적으면 예전처럼(0.75 vs 클립 0.875)
  마지막 프레임을 아무도 못 보는 일이 다시 생깁니다
- `EnemyBoss` — 전환 **시작** 신호, 전환 중 스프라이트 숨김/복귀
- `CameraShake` (신규) — 지금 흔들림 코드가 **하나도 없습니다.** 내려찍기·재 폭발에도 씁니다
- `AshBossTransitionBuilder` (신규) — 슬라이싱 → 클립 → 파티클 프리팹 조립
- `BossHealthBar` — 탈색 → 재충전

파티클 주의점 셋:

- 소용돌이 축은 `Orbital Y`가 아니라 **`Orbital Z`** 입니다. 튜토리얼 대부분이 3D 기준인데
  이 게임은 XY 평면을 Z에서 내려다봅니다. Y로 돌리면 소용돌이가 옆으로 눕습니다
- 파티클 텍스처는 매끈한 원형 글로우가 아니라 **시트에서 잘라낸 재 조각**이어야 합니다.
  주변이 전부 픽셀 아트인데 파티클만 매끈하면 그것만 붕 뜹니다
- "재가 원형으로 모여든다"는 `ParticleSystemForceField`의 **음수 Gravity**로 만듭니다.
  `Rotation Speed` + `Rotation Attraction`을 같이 주면 소용돌이가 됩니다. 코드로 위치를
  계산할 필요가 없습니다

### 2. 열쇠 개수 HUD

재 게이지를 왼쪽으로 옮기면서 **오른쪽 위가 통째로 비었습니다.** 그 자리입니다.

### 3. 사운드 시스템

`AudioSource`/`AudioClip` 사용처가 프로젝트에 **하나도 없습니다.**
`Assets/Project/Audio/Resources/GameAudioMixer.mixer`를 만들고 그룹 `Master`/`Bgm`/`Sfx`,
노출 파라미터 `MasterVolume`/`BgmVolume`/`SfxVolume`을 맞추면 설정 창의 볼륨이 그대로 붙습니다.

### 4. 스탯 표기

`Health`/`PlayerStamina`의 기본 최대치 + 유물 보정, `SkillController`의 `BonusDamage`와
`CooldownScale`이 이미 다 계산돼 있는데 어디에도 안 보입니다.

### 5. 밸런스

- 보스까지 17방 — 열쇠 4개 × 25%면 기대 상자 16개. 스코프의 "1회 8~12분"과 안 맞습니다
- ~~보스 체력 40 — 2페이즈(20)가 3~6초~~ → **2026-09-02에 체력 90 / 전환 0.7로 바꿨습니다.**
  2페이즈가 63을 맡아 예전(20)의 3배가 됐습니다. 2026-09-04 녹화에서 생존 3분 51초 /
  처치 60으로 한 판을 돌았고, 2페이즈에서 재 폭발이 여러 번 나오는 것까지 확인했습니다

### 6. 승리 흐름 완주 검증

튜토리얼 → 던전 → 열쇠 4개 → 부서진 문 → 보스 → 유물 → 문 → 결과.
~~코드는 다 연결됐으나 **끝까지 도달한 적이 없습니다.**~~ → **2026-09-07에 완주했습니다.**

**2026-09-06 — 배선을 전수로 짚었습니다. 끊긴 곳이 하나도 없습니다.**

| 고리 | 상태 |
| --- | --- |
| `Room_Boss.encounter` → `BossEncounter` | OK |
| `Room_Boss.reward` → `BossRelicReward` | OK (`clearRelic` = `Relic_RoadKnot` "길의 매듭"(09-26까지 `Relic_AshKingHeart` "재의 왕의 심장"), role 2 = RunEnd) |
| `Room_Boss.roomDoor` → `RoomDoorState` | OK |
| `Room_Boss.exitTrigger` → `RoomExitTrigger` | OK |
| `RoomSequenceController.bossRoom` → `Room_Boss` | OK (`bossClearEndsRun: 1`) |
| `RunManager.result` → `RunResultData` | OK |
| 빌드 세팅 | OK (Title / Game / Result 셋 다 등록) |

코드 경로도 이어져 있습니다 — 보스 사망 → `EncounterCleared` → 유물 드롭 →
`RelicInventory.Gained` → 문 개방 → `ExitRequested` → `EndRun(true)` → `LoadResult()`.

**즉 설정이 빠진 것이 아니라 아무도 끝까지 가보지 않은 것입니다.** 실제로 막고 있던 것은
거리 하나였습니다 — 보스까지 17방. `RoomSequenceController`에 B키가 있지만
**`enableDebugKeys`가 씬에서 꺼져 있어 안 먹습니다.**

그래서 디버그 패널(F1)에 `보스 방으로` 버튼을 넣었습니다. 그 토글과 무관하게 동작합니다.
확인 경로는 이렇습니다:

```
F1 → 보스 방으로 → 보스를 2페이즈 직전으로 → 직접 때려서 전환 확인 →
보스 처치 → 유물 줍기 → 문으로 나가기 → 결과 화면
```

**남은 청소 하나:** `Room_Boss / RewardChest`에 `BossRelicReward`가 하나 더 붙어 있습니다.
오브젝트가 비활성이고 `Room_Boss.reward`는 자기 자신을 가리키므로 아무도 안 부릅니다.
상자를 복사해 와 컴포넌트를 바꿔보다가 방 본체에 붙이기로 하고 꺼둔 흔적입니다.
지금은 무해하지만 **판을 끝내는 유물을 떨어뜨리는 컴포넌트**라 지우는 편이 낫습니다.

**2026-09-07 — 한 판을 끝까지 돌았습니다. 결과 화면까지 나옵니다.**

```
보스 방으로 → Room_Boss 입장 → 2페이즈(체력 64/90, 임계 63) → 보스 처치
→ 보상 등장 → 클리어 유물 드롭 → 획득 → 문 개방 → 출구 판정 켜짐
→ 출구 진입 → 보스 방을 나갔다(클리어) → [결과] 생존 31.5초 / 처치 1 / 1번째 방 / 클리어
```

로그에 `[출구/강제]`(ForceExit)가 없습니다 — 강제로 울린 것이 아니라 **진짜 트리거가
먹었습니다.** 배선뿐 아니라 이벤트와 물리까지 실물로 확인됐습니다.

**남은 것은 하나입니다 — 걸어서는 출구에 못 닿습니다.** 진입 로그가 디버그 패널의
`문 앞으로 (판정 한가운데)` 바로 다음 줄에만 찍혔습니다. 보스 방 출구는 **y 9.8~12.0**,
튜토리얼은 **y -13.2~-11.0**인데 플레이어 몸 콜라이더는 발밑 1.25유닛짜리입니다.
판정도 이벤트도 멀쩡하니 남은 것은 위치입니다. **방마다 출구 높이가 다르므로 한 방만
맞추면 다른 방이 어긋납니다.**

**로그 문구 하나가 틀렸습니다.** `[유물 획득] 재의 왕의 심장 (None +1) — 보관함 1개,
장착 칸이 가득 참`. 값은 전부 정상입니다(`effect: 7` = `None`, `role: 2` = `RunEnd`).
하지만 표식 유물은 `RelicInventory`가 **일부러** 장착에서 빼는 것이지 칸이 찬 것이
아닙니다. 보관함에 1개뿐인데 "가득 참"으로 읽혀서, 나중에 없는 버그를 쫓게 됩니다.

### 7. 그 밖에 남은 것

- 창 크기·전체화면은 **빌드에서만** 확인됩니다
- **카메라 상수 불일치** — `AshProjectSetup.CameraOrthographicSize`는 5.625인데 `Game.unity`는 14입니다.
  그 도구를 다시 돌리면 카메라가 확 당겨져 보스 방 구도가 깨집니다
- ~~`Skill_Q_GroundSlam`의 damage가 0입니다~~ → **버그 아님 (2026-09-06 확인).**
  `GroundSlamSkillData`는 상속받은 `SkillData.Damage`를 한 번도 읽지 않습니다. 데미지가
  1단·2단으로 갈라져 `nearDamage 2` / `farDamage 5`가 전부를 담당하고, 붙어서 맞히면
  7이 정상으로 나갑니다. 나머지 네 스킬은 `Damage`를 실제로 쓰므로 값이 있습니다.
  **그 0을 채우면 동작하는 것처럼 보이는 죽은 값이 늘 뿐입니다** — 코드에 주석으로 박아뒀습니다
- 이동은 복합 바인딩이라 리바인딩에서 빠져 있습니다. 패드 리바인딩도 범위 밖입니다

- `FixWalkCycleSheet.ps1` — 걷기 6프레임 시트의 다리 대칭과 상체 바운스를 후처리로 만든다.
  생성 시트는 상체를 픽셀 단위로 공유시켜 정체성은 지키지만 **몸이 한 프레임도 안 움직인다**.
  다리는 뒤 세 프레임을 앞 세 프레임의 좌우 반전으로 바꾸고, 상체는 프레임마다 1~2px 올린다.
  **검(주황 잉걸)은 색으로 걸러 반전에서 뺀다** — 상체의 일부라 뒤집으면 허공에 조각이 뜬다.
  대칭축은 다리 무게중심 평균에서 얻는다(그림이 아니라 동작에서 축을 얻는 방식).
